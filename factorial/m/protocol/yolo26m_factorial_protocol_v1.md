# YOLO26m factorial protocol v1

Frozen before any factorial result: **yes**.

## Question and outcome-neutral objective

How does the Unity-minus-Duplicate × adaptation-scope interaction behave across YOLO26n, YOLO26s, and YOLO26m? No sign is preferred. The designated endpoint is the xView-test vessel AP50–95 Marginal DiD with the frozen descriptive M1–M5 hierarchy.

## Design

Duplicate/Unity Medium150 × semantic head-only/full-network × LR 1e-4/2e-4 × seeds 20260723–20260732. All 80 fresh cells start from the one locked xView YOLO26m checkpoint, use batch 16, AdamW, 20 epochs, deterministic mode, and epoch-20 `last.pt`.

## Schedule and evaluation

Execution is blocked by seed and randomized within seed using RNG 20260831. Evaluation order is xView test, HRSC2016-MS, DIOR. Full-precision per-image predictions are retained.

## Classification and boundaries

Classification precedence is M5, M1, M2, M3, M4 using the exact criteria in the analysis manifest. This is a prospectively frozen within-study third-model-size follow-up on previously used evaluation sets, not independent or globally untouched confirmation. No v48 edit, YOLO26l, same-scale Support High/Low, dose response, transformer detector, new LR/freeze depth, or post-result retuning is authorized.
